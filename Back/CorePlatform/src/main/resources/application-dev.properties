# Development settings - local DB, permissive and verbose
spring.datasource.url=jdbc:postgresql://localhost:5432/coredbcrm
spring.datasource.username=postgres
spring.datasource.password=montufar

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Dev conveniences
app.seed.enabled=true

# Dev RabbitMQ / JWT (placeholders)
spring.rabbitmq.host=localhost
spring.rabbitmq.port=5672
spring.rabbitmq.username=guest
spring.rabbitmq.password=guest

jwt.secret=dev-secret
jwt.expiration-ms=3600000

# Logging and management for debugging
logging.level.root=DEBUG
logging.level.com.krendom85.coreplatform=DEBUG
management.endpoint.health.show-details=always

# Permissive CORS for local frontend dev
cors.allowed-origins=*